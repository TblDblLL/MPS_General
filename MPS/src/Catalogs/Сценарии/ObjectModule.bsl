Процедура ОбработкаПроверкиЗаполнения(Отказ, ПроверяемыеРеквизиты)

	Если не НачалоПослеЗапуска Тогда
		ПроверяемыеРеквизиты.Добавить("ДатаНачалаВыполнения");
	КонецЕсли;

	Если не ОстановкаВручную Тогда
		ПроверяемыеРеквизиты.Добавить("ДатаОстановки");
	КонецЕсли;

КонецПроцедуры